# Arc Federal-Grade Roadmap — Execution Program

**Orchestrator:** Fable (this shell). **Workers:** Opus (design/security/implement/review) · Sonnet (docs/changelog/version/mechanical).
**Flow per spec:** `/specify` (Opus) → `/deepen` (Opus) → `/implement` (Opus, verify each) → `/review` (Opus) → docs (Sonnet) → PR to **develop**.
**Branching:** per-spec `feat/SPEC-NNN-*` off `develop` → PR to `develop`. After a phase's specs land in develop → one promotion PR `develop → main`.
**Baseline:** develop = 72aed05 (SPEC-031 + live-daemon fixes). Roadmap artifact: https://claude.ai/code/artifact/7612c927-9d83-4f26-b9b1-00aba8b5edd2
**Governing constraints (every spec):** clean · very-few-LOC (delete-as-you-add, WIRE don't rebuild) · secure-by-construction · config-relaxable tiers · EXTENSIBLE (extension points: brain/retrieval, skills, tools, hook-builds) · self-modifying · blueprints/preset-configs.

Status: ⬜ pending · �iP in-progress · ✅ done+merged-to-develop · 🚀 promoted-to-main

---

## ▶ RESUME AFTER CLEAR (read this first, then CLAUDE.md governs)
**Who:** THIS shell = **Fable, the ORCHESTRATOR**. Do NOT write code/specs here — spawn **Opus** workers (specify/deepen/implement/adversarial-review/fix) and **Sonnet** workers (docs/changelog/version/mechanical). Verify EVERY worker independently; never trust "done/green" self-reports.
**North star (Josh):** a COMPLETE harness — minimal lines of code, simple, fully secure, **FedRAMP-ready for federal agencies**. See [[project_arc_federal_vision]].
**Flow per spec:** `/specify`(Opus) → `/deepen`(Opus) → `/implement`(Opus, TDD) → **adversarial `/review`(Opus)** → fix findings(Opus) → docs(Sonnet) → PR to **develop**. Per-spec branch off develop; promote develop→main per phase.
**Verify-don't-trust (hard-won, [[feedback_producers_unwired_pattern]]):** (1) EVERY spec ships mechanisms correct but activating wiring DEAD — probe "is the producer wired end-to-end through the REAL production path?" and demand a test that drives it (tests repeatedly proved a path prod doesn't use, or a rigged fixture). (2) Run the FULL package matrix (ALL 10+ packages incl arcstore/arcmemory/arcui + root arch) before every merge. (3) Run `scripts/check_loc_budgets.py` every merge (arcagent core <3500 — MOVE code out, don't raise ceiling, [[project_loc_budgets_signal_design]]). (4) NO parallel repo-mutating workers in the shared tree (worktree isolation or sequential — [[feedback_subagents_no_destructive_git]]). (5) no-legacy delete-as-you-add; commit with `--no-verify` (pre-commit hook runs full suite & times out — matrix verified separately).
**State:** develop @ 71d897d. Phase 1 (security foundation, 8 specs) 🚀 ON MAIN. Phase 2 on develop: SPEC-040 planner ✅ · SPEC-043 loop-controls ✅ · SPEC-041 arcmemory ✅ · SPEC-044 skill improver ✅ (PR #17) · **SPEC-047 extensibility+blueprints ✅ (PR #20)**. Also landed on develop via another session: simplification-sweep (PR #18, refactor).
**Josh's "build SPEC-044 then SPEC-047" ask is COMPLETE.** NEXT (per roadmap order): independent track SPEC-045 (scale+interop, spec'd+banked in `.claude/specs/SPEC-045-scale-interop/`) → 032 (mission control) → 046 (rogue-agent) → 042 (autonomy) → 054 (trace replay). Then → PROMOTE develop→main (Phase 2).

## P0 — hotfix (fast-track, no /specify)
- ✅ **P0** CVE-2026-39892 patched (crypto 46.0.7) + 10 dep bumps + blocking harness-scoped CI gate + POA&M + jupyter excluded. **PR #4 MERGED → develop** (f34ed42).

## Phase 1 — Make it true (security foundation) · 🚀 PROMOTED TO MAIN (f66f297, PR #13, 2026-07-07) — all 8 specs + P0 live on main
- ✅ **SPEC-036** Real tier-enforced code-exec sandbox (ASI05) — VmBackend/Firecracker+jailer, fail-closed router, personal sandbox-off=host access, container-exec proven live, audit-attributed. **PR #5 MERGED → develop** (dcff0d4). arcrun 0.6.0, arccli 0.4.1.
- ✅ **SPEC-033** Enforce Sign pillar — verify-at-load + restricted-builtins on EVERY agent-writable root (workspace/global/agent); federal=signed AND operator-approved; skill-folder SKILL.md gated; WORM audit outside workspace + chain-verify IR alert; arctrust.artifact sign/verify; os_sandbox deleted. Review found+fixed 6 (2 reachable bypasses: root-scoped gate CRITICAL, federal self-signed auto-allow HIGH). **PR #6 MERGED → develop** (516835a). arcagent 0.5.0, arctrust 0.4.0, arcskill 0.1.1.
- ✅ **SPEC-034** Complete policy pipeline — Provider/Team/Sandbox layers real (pure comparators over PolicyContext) + worm_policy_sink audit adapter wired into build_pipeline. Configured-gate fail-closed (Josh's call: no policy configured → ALLOW no-op; configured-but-blind → DENY) — fixed a defect that bricked ALL enterprise/federal tool calls by default. Review: layer logic clean; found 1 HIGH audit-authority flaw → SPEC-053. **PR #7 MERGED → develop** (4807cc2). arctrust 0.5.0.
- ✅ **SPEC-053** 🔒 Audit-authority independence (Josh: tiered) — `OperatorKey` (non-AgentIdentity) signs ALL 4 WORM sinks; agent DID signs only attestations; `arc init` key; federal WitnessAnchor. Folded findings 2+6. Improver: audit→operator, skill-sig stays agent DID. Review found custody defeatable (bash-readable seed + illusory witness + silent regen) → ALL custody defects fixed before merge (fail-closed no-regen, verify_inclusion wired + fail-closed federal witness, atomic bootstrap, symlink/perm hardening, seed only to WORM modules). **PR #8 MERGED → develop** (73abc86). arctrust 0.6.0/arcagent 0.6.0/arccli 0.5.0. **Closes its share of the composite HIGH-close; SPEC-035 + SPEC-037 still required before promotion.**
- ✅ **SPEC-035** Lock goals + lethal trifecta — protected-path denylist (identity.md/policy.md); wire ForbiddenCompositionChecker + EgressProxy + human-gate. [arcagent] · ASI01 · **+ from SPEC-033 review: `bash` is raw host exec with NO path confinement — an agent can write anywhere on the host (the write-vector behind the sign-gate bypass). At enterprise/federal, route `bash`/shell tools through the SPEC-036 sandbox (workspace-only mount) or confine writes to workspace+allowed_paths; personal = host access OK.** · **+ from SPEC-053 review (CRITICAL, part of the audit-forgery HIGH-close composite): `bash` + all subprocess/file transports MUST be confined away from `~/.arc/operator/**` (operator audit key) and `.audit/**` (WORM chains) — else agent bash reads the operator seed and forges the audit log exactly as before. This confinement is REQUIRED for SPEC-053 to actually hold at enterprise/federal.**
- ⬜ **SPEC-037** Asymmetric + FIPS signing — Ed25519/ECDSA (replace HMAC stub); ✅ **SPEC-037** Asymmetric + FIPS signing + out-of-process key custody — Signer Protocol (InProcess/Vault); dual-algo Ed25519 default + ECDSA-P256 FIPS (additive); HMAC deleted (arcllm+arcteam → asymmetric); generalized FIPS gate; `WormSink` takes Signer. Review found the primitive UNWIRED (custody dead knob, require_fips tier-decoupled, chains hardcoded Ed25519, messaging re-signed with agent DID) → ALL fixed: federal now FORCES require_fips+vault_transit+ecdsa-p256 (rejects weaker); seed out of agent process (FileNotaryTransit child, `_operator_key is None`) — proven E2E. **PR #10 MERGED → develop** (9b69a15). arctrust 0.8.0/arcagent 0.8.0/arcllm+arcteam bumped. **CLOSES the audit-forgery HIGH composite (053+035+037).**
- ✅ **SPEC-038** Budgets + classification binding — wire dead cost/token budget to API (LLM10); classification identity-bound + propagated across delegation+messenger. [arcrun/arcteam/arcllm] · *SPEC+DEEPEN done, IMPLEMENTING (feat/SPEC-038 off 9b69a15). Josh OQ defaults: lift arcteam ladder (defer compartments); federal fail-closed on missing clearance; token-primary budgets w/ per-tier default ceilings; both tool+entity labels no-read-up; single external ceiling no-exfil. Fills SPEC-034 provider_usage seam + activates SPEC-035 trifecta.* · **+ messenger/comms tools MUST tag `external_comms` leg + route through EgressProxy (SPEC-035 trifecta).** **+ populates PolicyContext.provider_usage (SPEC-034 ProviderLayer). From SPEC-034 review: unknown provider-label with limits configured must FAIL CLOSED (else budget dodge by label); provider label must come from a trusted non-agent source.**
- ✅ **SPEC-039** Truthfulness pass — READMEs/docstrings→reality; refresh roadmap.md; sign arcagent modules or drop claim. [all · Sonnet-heavy] · **🔒 + HARD promotion-gate items: (1) core-slim — arcagent/core NCLOC is OVER budget (3579 now / was 3534 on develop after SPEC-037; budget 3500). MOVE non-nucleus code out to modules/utils, do NOT raise the ceiling ([[project_loc_budgets_signal_design]]). Process miss: SPEC-037 breached it and I merged w/o catching — verify core LOC on EVERY future merge. (2) honest trifecta docs (armed/dormant nuance). (3) OQ-3: ship conservative per-tier default budget ceilings (federal non-relaxable) — mechanism live but no baked-in constants yet.** · **+ from SPEC-033: 8 pre-existing env-driven `mypy --strict` errors in arcskill `hub/` (sigstore/docker optional-extra imports, 2 unused type:ignore, 1 attr-defined). arcskill has NO mypy gate — add a `[tool.mypy]` config (module overrides for optional extras) so arcskill passes strict like the other packages.** **+ from SPEC-053: pre-existing pytest collection collision — arctrust/tests/conftest.py and arccli/tests/conftest.py both import as `tests.conftest` (each `tests/` has `__init__.py`), so a 3-package `pytest arctrust arcagent arccli` fails at collection even with `--import-mode=importlib`. Per-package runs pass. Fix = conftest/test-package naming across packages (broad, risky — own ticket).**
- **→ PROMOTE develop→main (Phase 1)** · ✅ SECURITY GATE CLEARED — audit-forgery HIGH-close composite COMPLETE: SPEC-053 ✅ (operator-key separation, hardened custody, verified fail-closed federal witness) + SPEC-035 ✅ (bash confined off `~/.arc/operator/**` + `.audit/**`) + SPEC-037 ✅ (seed out of process memory, federal forces vault_transit). Remaining before promote: SPEC-038 + SPEC-039. · 🔒 QUALITY GATE: core NCLOC < 3500 (currently OVER at 3579 — SPEC-039 core-slim must clear it); all packages ruff+mypy-strict clean.

## Phase 2 — SOTA + mission control
- ✅ **SPEC-040** Real planner — Plan-Execute + LLMCompiler DAG, durable/resumable, bounded replan, policy+budget gated, operator-signed audit, identity-goal-drift refused. Review found the PRODUCTION step-executor was BLIND (RunResult carried no terminal outcome → every step reported SUCCEEDED; replan+audit falsified; E2E tests used a test-only seam) → fixed on production path (RunResult surfaces completion_payload; aggregate budget; planning→WORM set; plan-file signed; goal-hash→identity.md). **PR #14 MERGED → develop** (04dcba1). arcagent 0.11.0 + arcrun RunResult terminal outcome. LOC 3441/3500. Full matrix green. **Lesson: tests proved a path prod didn't use — [[feedback_producers_unwired_pattern]] #5.**
- ✅ **SPEC-041** ~~Close learning loop~~ → **arcmemory: dual-speed analogical memory** — **MERGED → develop PR #16 (29fd417)**. Built all 11 phases (8 commits); review caught 2 headline features broken-in-prod (no-read-up gate blind to episodic/insight stores = classified leak; centerpiece was LEXICAL not structural — abstraction never reached the Brain.retrieve seam + rigged AC-6 test) + BYO-brain RCE + distiller budget → ALL fixed & re-proven (SECRET dropped on episodic/insight/daily-log fail-closed federal; genuine zero-token-overlap structural match via graph, mechanically guaranteed). arcagent MEMORY-LESS by default (Brain Protocol+NullBrain in arcagent/brain/, zero memory files; arcmemory or BYO = plugin = SPEC-047 seam delivered). Both old backends deleted. arcllm.embed added. Also fixed repo-wide .gitignore swallowing every package's tests/architecture/. new arcmemory 0.5.0/arcagent 0.13.0/arcllm 0.7.0. **UNBLOCKS SPEC-044 + SPEC-047.** — **RE-SPEC'd 2026-07-07** (folder renamed `SPEC-041-arcmemory`; supersedes the old learning-loop-in-arcagent-modules scope). NEW **`arcmemory` package** (→ arctrust/arcllm/arcstore; sibling of arcrun; arcagent wires via a `Brain` Protocol + hooks + proactive schedule — the SPEC-047 pluggable-brain seam, delivered). Two speeds: deterministic zero-LLM fast capture (FERNme Hebbian) + scheduled LLM "sleep" consolidation. Four stores: episodic/semantic/procedural/**insight**. Retrieval = fused **surface** (sqlite-vec+BM25+graph RRF, recency) **+ structural/analogical** (minted-offline abstraction: trigger-embed + cue-graph spreading activation, MAC/FAC-shaped, HippoRAG-style enrichment) — the centerpiece (retrieve a recurring pattern/thesis with **zero surface overlap**, then enrich). Absorbs bio_memory facts/entity/consolidator/retriever + memory FTS5; **deletes both backends** + dead artifacts (no-legacy). Retains prior 041's no-read-up gate, boundary-marking, sanitize, reflection→ACE, goal-lock. Research done (FERNme/mem0/AgentMemory/Hermes/ACE/Reflexion/structure-mapping). **SPEC+DEEPEN DONE 2026-07-07** — 6-agent deepen added concrete constants (decay/salience/confidence table R-9), analogical params (ACT-R fan-effect spreading + MAC/FAC + conjunctive gating R-8), consolidation pattern (bounded single call, event-triggered R-10), sqlite-vec ceilings + embedder R-11, rerank/HITL R-12, and **in-repo seam verification (DC-1..5) that caught producers-unwired risks**: arcstore CANNOT host FTS5/vec (closed 5-kind spool) → arcmemory owns its own per-agent SQLite; `@background_task` is interval-not-cron; no `post_respond` (it's `agent:pre_respond`); sanitize is arcagent-util not arctrust; `audit.emit` needs explicit sink; `arcllm.embed` is vaporware (build it). **All 7 OQs resolved** (D-1..D-10 in README): arcmemory owns index · tier-varied constants · bge-small-en-v1.5 embedder (ONNX) · cross-encoder bge-reranker in v1 (personal OFF+margin-fallback / ent+fed ON). **Unblocks SPEC-044 + SPEC-047.** Next: `/implement` (Phase-0 scaffold first). SPEC-044/SPEC-047 remain HELD until this lands; memory-INDEPENDENT specs (043/045/032/046/042) continue in parallel.
- ⬜ **SPEC-042** Turn on autonomy — wire proactive engine (heartbeat wake, leader election); retire pulse/scheduler overlap. [arcagent]
- ✅ **SPEC-043** SOTA loop controls — checkpoint/resume; tool-based HITL; unified circuit breaker; wire parallel_dispatch; Plan-Execute Strategy. **PR #15 MERGED → develop** (d765f3e). arcrun 0.9.0/arcagent 0.12.0. Review: core mechanism sound (no race, no HITL bypass) but controls only wired in dispatch_stream → fixed UNIFORMITY (build_loop_controls helper on ALL run paths so none can forget; resume wired E2E; checkpoint operator-signed). Replay/time-travel split → SPEC-054. *[history: SPEC+DEEPEN, IMPLEMENTED feat/SPEC-043 off 04dcba1. Josh steered: **STREAMING CUT** (agentic harness wants output not tokens; delete fake word-split, keep TurnEnd/collect/RunResult unchanged) — 6→5 controls. **HITL = tool-based** predicate (arcrun dumb membership test; arcagent resolves per-tier set + marks skill-backed tools). Josh's tier ladder: **personal=no approvals (opt-in via config); enterprise=approve EVERY tool; federal=approve EVERY tool+skill**. Reuse SPEC-035 token; arcrun mints nothing. THE hard part = concurrency-safe accounting (per-session lock atomic snapshot→evaluate→record + reserve-then-settle budget; interleaving-forced tests must fail on unguarded). Wires dead parallel_dispatch + closes SPEC-040 deferred parallelism via Plan-Execute strategy. OQ-2=caller-fans-out (StepExecutor iface unchanged); OQ-4=turn-boundary checkpoint + deterministic resume (no side-effect re-run). Known limitation: mid-turn crash re-fires that turn's tools on resume — mitigated (destructive tools are HITL-gated → rare+human-approved); per-tool checkpointing for destructive tools is a future add if needed.*
- ✅ **SPEC-044** Best skill builder/improver — **PR #17 MERGED → develop (213e4df)**. Improver relocated arcagent→`arcskill.improver` (no-legacy, agent core −2,400 NCLOC, core 3498/3500); **Josh's package ruling: arcskill IS the optional supercharger** (arcagent runs skills alone; a mid-program worker misread spawned a phantom `arcevolve` package — caught+reverted, [[feedback_close_workers_before_next_phase]]). ONE integrated method: GEPA trace-reflection + SkillOpt bounded-edit (arXiv:2605.23904 verified; Lt 8/4/2 federal-floor, cosine decay, rejected-edit buffer); golden-task strict-improvement gate (judge ranks only, ≥3 pytest cases unlock code mutation) in arcskill's own hub.dry_run sandbox; nudge→usage→retire via 30-day config-adjustable Curator sweep (@background_task; ProactiveEngine→SPEC-042); retire=registry suppression (reversible+lineage); HumanGate operator approval (federal every mutation+retire fail-closed); operator-key WORM audit / agent-DID artifacts; SkillAdapter seam = SPEC-047 extension-point shape delivered. Review found 3+1 producers-unwired blockers (sweep unwired, HITL absent, retire hollow, registry seam test-double-only) → ALL fixed & re-proven through real paths (AC-2 real-Docker E2E). Also fixed: pre-existing arcgateway-slack test hang (live network in unit test); __version__ drift → metadata-derived. Known pre-existing: arcui chat-WS echo test hang (separate session investigating). arcskill 0.2.0 / arcagent 0.14.0. Full matrix green.
- ⬜ **SPEC-045** Scale + interop — NATSExecutor + mTLS; ingress guardrails + rate/turn budgets; fix Slack/Mattermost agent_did; MCP client+server (anti tool-poisoning). [arcgateway/arcllm] · **+ from SPEC-035 review: network/egress tools (MCP, Slack, web-fetch, gateway) MUST tag `external_comms`/`untrusted_input` capability legs + route outbound through `EgressProxy` — else the SPEC-035 lethal-trifecta gate stays dormant (it's armed but has no leg-producers among current built-ins).**
- ⬜ **SPEC-032** Mission control — arcstore directory + arcui (roster/channels/fleet) + human DM/channel + membership control + task delegation/monitoring + gateway↔arcteam bridge. [arcteam/arcui] · *SPEC + DEEPEN already done on disk; resume at /implement*
- ⬜ **SPEC-046** ASI10 rogue-agent controls — anomaly detection; policy-violation alerting; agent-level kill-switch + revocation. [arcagent/arctrust]
- ⬜ **SPEC-054** Trace replay / time-travel governance (NEW, Josh 2026-07-07 — split from SPEC-043 OQ-4; distinct from resume=crash-recovery). Re-run a step with FROZEN upstream context (Langfuse/LangSmith/Laminar pattern); step-through the durable versioned trace. **arcstore** durable versioned trace (arcui data source) + **arcui** step-through/re-run surface + **arctrust** provenance/audit (AU-2/AU-3/AU-10, CM-8 tool/skill version inventory). Prereq: version-stamp tools/skills + arcrun emits versioned step provenance (was SPEC-043 REQ-007, moved here). LLM-step re-run free; side-effecting-tool re-run sandboxed/gated. **Queued after SPEC-032** (shares arcui/arcstore). [arcstore/arcui/arctrust] · NOTE: replay ≠ SPEC-044 (skill improver).
- ✅ **SPEC-047** Extensibility + blueprints framework — **PR #20 MERGED → develop (71d897d)**. Generalized the two delivered seams (Brain/041, SkillAdapter/044) into ONE `ExtensionPoint`+`select_extension` (deleted the duplicated dispatch/BYO-gate/importer, behavior-preserving — brain/skilladapt suites unchanged); 4 families (brain+skills select-one, tools+hook-builds scan-many over existing SPEC-021 loader — no new engine); signed TOML blueprints (3 packaged presets provenance-trusted in-wheel + user `~/.arc/blueprints` `.arcsig`), **materialize-to-disk** (DC-8b: runtime __main__ flat-loads, a runtime merge layer would be DEAD — caught in deepen), user-config-wins precedence, stringency-max tier (blueprint can only RAISE); `arcagent/tiers.py` RelaxableKnob table + resolve_tier_floor the SecurityConfig validators delegate to (**net −20 core NCLOC**); arccli `arc blueprint` + extended `arc ext inspect/verify` + `init --blueprint`; `open`→`personal` vocab unified. Josh's 4 calls: materialize-to-disk+user-wins, personal-everywhere, extend-not-collide ext, federal=operator-provisions-signed/agent-cannot-self-add. Review drove all mechanisms real-path (federal floor holds thru malicious blueprint, BYO-refuse survived dedup, presets ship in built wheel — NO core producers-unwired defect) + found 1 HIGH (blueprint sig was TOFU not operator-pinned → attacker-self-signed preset accepted at federal; capability_loader "unpinned floor is no floor" precedent) + MED (dry-run false WORM audit) + LOW (custody denylist) → ALL fixed+driven-tested. Merged develop's simplification-sweep (PR #18) mid-flight — 1 additive ext.py conflict resolved, full suite green post-merge. arcagent 0.15.0 / arccli 0.6.0. **"build SPEC-044 then SPEC-047" COMPLETE.**
- **→ PROMOTE develop→main (Phase 2)**

## Phase 3 — Federal moat (mostly non-code, Sonnet+Opus mix)
- ⬜ **SPEC-048** ATO package — SSP (OSCAL), authorization-boundary diagram, POA&M, ConMon plan, system-wide 800-53 mapping (promote arcrun pattern). [compliance]
- ⬜ **SPEC-049** Live AI-BOM — CI-generated (models/datasets/MCP/skills, CISA/G7 elements) + model-weights provenance attestation. [arcstore/CI]
- ⬜ **SPEC-050** FIPS validation evidence — FIPS-OpenSSL AES-GCM test/doc; validated Ed25519/ECDSA path or scoped module (SC-13/IA-7). [arctrust]
- ⬜ **SPEC-051** Standards positioning — map DID+delegation to NIST NCCoE agent-identity concept paper; be the reference impl. [docs/strategy]
- ⬜ **SPEC-052** Easy button + DX — example agent configs, getting-started, module-walkthrough polish, resolve two-form module authoring. [arccli/docs]
- **→ PROMOTE develop→main (Phase 3)**

---

## Live directives (from Josh — apply at the named spec)
- **SPEC-036 (sandbox):** personal tier MUST be able to turn sandboxing fully OFF — `isolation="none"`, full host/computer access — via explicit config (Josh wants his own agent to access his machine). Default personal = container; the OFF switch is a first-class supported mode. Enterprise/federal floors stay non-relaxable. → amend PRD/SDD REQ after deepen returns.
- **SPEC-049 (AI-BOM) + SBOM generally:** scope the SBOM/AI-BOM to the ACTUAL HARNESS runtime only. EXCLUDE jupyter/notebook, tutorial, help-docs, and walkthrough tooling — those aren't part of the harness and only confuse what the government evaluates. Harness deps only. (P0's SBOM regen is interim; SPEC-049 does the proper harness-scoped AI-BOM.)
- **Self-adaptation (SPEC-041 learning loop · SPEC-044 improver · SPEC-047 extensibility):** /deepen MUST research **how Hermes adapts / self-improves** (the Hermes agent harness — the one arcgateway lists among supported harnesses; identify the correct project first) and **present options** for adopting its approach. Architecture intent: self-adaptation capability lives **in arcskill** AND is exposed as an **optional, core extension to the arcagent harness** (pluggable "brain"/adaptation extension per the SPEC-047 extension-point model — config-enabled, not hardwired). Present the Hermes-derived options in the relevant SDD's Research Insights so Josh can pick.
- **SPEC-044 (skill improver):** fold in Microsoft's **SkillOpt** paper — bounded per-iteration change. Add **configurable change-bound settings** ("how much a skill may change per optimization step") as first-class config, tunable per tier/skill, on top of the existing anchor-distance/token-budget/oscillation guardrails. /deepen MUST research the SkillOpt paper (Microsoft, 2025-26) and adopt its bounding method; frame as bounded/auditable self-modification (federal-friendly). Hypothesis to validate in deepen: tighter, tunable bounds improve convergence + result quality.

## Log
- 2026-07-09: 🎯 **SPEC-044 MERGED (PR #17, develop @ 213e4df).** Full flow: specify→deepen (SkillOpt arXiv:2605.23904 verified real; Hermes=NousResearch; DC-5 caught nonexistent "VmBackend"→hub.dry_run)→Josh's calls (H-A+H-B 30-day sweep, Lt 8/4/2, ≥3 pytest cases)→implement (11 phases, 2 workers after session-limit handoff)→adversarial review (3 blockers: sweep unwired/HITL absent/retire hollow — the recurring producers-unwired class, 5th spec in a row)→fix research surfaced a 4th (registry seam matched only a test double in prod)→all fixed via ruled mechanisms (shared HumanGate, @background_task, registry suppression)→docs truthfulness (undocumented seam + version drift caught). **Process incident:** deepen worker kept editing spec files after phase-done on a misread of Josh's direct steering ("not add in arcskill"→invented `arcevolve` package) while the implementer built — Josh caught it; ruling: arcskill IS the optional supercharger. New protocol: explicit phase-close orders to every worker; verify any "per Josh" annotation with Josh ([[feedback_close_workers_before_next_phase]]). Pre-existing arcgateway-slack test hang fixed; arcui chat-WS hang documented (separate session). NEXT: SPEC-047.
- 2026-07-06: program kicked off; develop synced to 72aed05; P0 started; SPEC-036 specify done, deepen running.
- 2026-07-06: SPEC-036 shipped (PR #5, dcff0d4). SPEC-033 Sign pillar shipped (PR #6, 516835a) — security review caught 2 reachable bypasses (root-scoped gate, federal self-signed auto-allow) + 4 lesser; all fixed & verified before merge. develop @ 516835a. Next: SPEC-034 (policy pipeline) ∥ SPEC-035 (goals + lethal trifecta, now also owns bash-confinement).
- 2026-07-06: SPEC-034 policy pipeline shipped (PR #7, 4807cc2, develop @ 4807cc2). Verify-don't-trust caught a brick (blanket fail-closed denied all enterprise/federal calls) → Josh chose configured-gate. Adversarial review: layer logic clean, found HIGH audit-authority flaw (agent signs its own WORM log) → Josh chose tiered fix → **SPEC-053 created as Phase-1 promotion blocker**. Latent review findings tracked: isolation-half producer (SPEC-036 tool_runtime wire — currently NO dispatch-time isolation enforcement), provider-label fail-closed (SPEC-038), team unknown-role fail-closed + delegation-grant-required (arcteam team_scope producer). Next: SPEC-035 (goals + lethal trifecta).
- 2026-07-07: 🚀 **PHASE 1 PROMOTED develop→main (PR #13, main @ f66f297).** SPEC-039 merged (PR #12, 1fb32c7): core-slim 3579→3411 (moved tool_transport out of core — LOC script globs core/*.py non-recursively so core→core moves are net-zero; deleted dead tool_policy_layers.py), arcskill mypy gated, conftest collision fixed (4152 cross-pkg), per-tier default budget ceilings, truthfulness docs (6 READMEs; caught inverted arcagent capability-root signing claim). Full matrix green all 8 pkgs; LOC PASS. **Phase 1 security foundation COMPLETE + live on main.** Process learnings: (1) every spec shipped mechanisms correct but activating wiring DEAD — adversarial review caught each; probe "producer unwired" by default. (2) run FULL package matrix pre-merge (arccli layer-count test broke on SPEC-038, missed). (3) check core LOC every merge (SPEC-037 breached, missed). Next: Phase 2 (SPEC-040 planner …). Review: recurring "predicates correct, producers UNWIRED" — budget breaker unreachable through agent (run_stream no params, no config, build_pipeline no provider_limits), no-write-down + no-exfil gates with no honest input. ALL 7 wired E2E + proven through real agent path; federal fail-closed by tier. Core LOC 3579→3569 (still over 3500). Cross-pkg 3796. develop @ 0f1d77c. LAST spec: SPEC-039 (core-slim<3500 + truthfulness + arcskill-mypy + conftest + per-tier ceilings) → then PROMOTE.
- 2026-07-06: SPEC-037 MERGED (PR #10, 9b69a15). Review verdict: primitive built but UNWIRED — custody=vault_transit a dead knob (seed still in memory at federal), require_fips tier-decoupled, chains hardcoded Ed25519, arcteam messaging RE-INTRODUCED the agent-DID self-signed anti-pattern. All fixed: federal model_validator forces fips+vault_transit+ecdsa-p256 (live-verified reject-weaker); seed provably out of agent process (VaultSigner + FileNotaryTransit child). **AUDIT-FORGERY HIGH COMPOSITE CLOSED (053+035+037).** Phase-1 security gate cleared. develop @ 9b69a15. Next: SPEC-038 (budgets+classification) → SPEC-039 (truthfulness) → PROMOTE.
- 2026-07-06: SPEC-035 MERGED (PR #9, 5923336). Review found trifecta gate INERT (no leg-producers; synthetic-tag false-confidence tests) + goal-lock case-fold bypass reachable on macOS (string-eq not inode) → both fixed before merge (inode compare live-proven on APFS; EgressProxy wired + real-tool e2e; auto_approve exact-match; cache incl session_caps; ledger per-session ContextVar). Bash-confinement + human-gate crypto were clean. Trifecta armed-but-dormant until network tools tagged (SPEC-045/038). Composite gate: 053✅ 035✅ — only SPEC-037 (out-of-process signing) remains.
- 2026-07-06: SPEC-053 MERGED (PR #8, 73abc86). SPEC-035 spec done + IMPLEMENTING (feat/SPEC-035 off 73abc86): A goal-lock (protected paths incl identity/policy/context.md), B lethal-trifecta (forbidden_compositions was empty stub + ForbiddenCompositionChecker instantiated nowhere + _egress unwired → now live/wired + session-ledger accumulation + human-gate via arcteam channel, human-signed token), C bash→SPEC-036 sandbox + real workspace bind-mount (DockerBackend declared supports_bind_mount but mounted only tmpfs). OQ defaults: untrusted-input proxy=web-fetch · goal-lock advisory@personal · human-gate fail-closed/per-action/arcteam-surface. Findings: forbidden_compositions/checker/_egress all stubs-or-unwired confirmed.
- 2026-07-06: SPEC-053 implemented (8815420) — type separation + all 4 sinks rewired + folded findings all CLEAN. But adversarial review: the security property isn't delivered — operator seed readable by unconfined bash + resident in process memory (CRITICAL, relocates not closes the HIGH); federal witness illusory (co-located, not append-enforced, verify_inclusion never called in prod, fail-open); silent key regen = covert erasure; bootstrap race; symlink TOCTOU. Custody defects (#2–6 + #1-memory) → fixing IN SPEC-053 now. Full HIGH-close = COMPOSITE: SPEC-053 (custody) + SPEC-035 (confine bash off ~/.arc/operator + .audit) + SPEC-037 (out-of-process signing). Promotion gate updated to require all three.

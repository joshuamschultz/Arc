# Business reliability consolidation handoff

Source is closed and frozen at code checkpoint `06b80bad0035cc0716b08592b2c6ac241a48632a`; integration is handed to root. The memory source merge is `d8e32a6f`, refreshed assets are `dcbd3c18`, and the checkpoint ancestry includes `02ebb651` and `06b80bad`. Root verified the integrated tree is identical to `dcbd3c18` and every local Arc branch tip is an ancestor of `06b80bad`. Movement to main and branch/worktree cleanup are next and remain pending root verification; do not claim either is done. This note records remaining acceptance work, not production or release acceptance. Resume from resulting main; no old branch or worktree is required.

## Targeted integration evidence

- Arc Cloud documentation commit `05ee143` is on cloud `main` above source merge `23e7fbc`.
- In `integrate-all`, targeted import/integration suites passed: UI/auth/chat/CLI batch, 91 tests; loopback-retry `test_cli_up`, 22 tests; skills/report/queue batch, 63 tests. The `test_cli_up` run overlaps one test with the UI/auth/chat/CLI batch; these counts are separate runs, not 113 unique tests.
- A direct `create_app` probe without the account factory returned HTTP 200 for static-token auth `/me` and HTTP 503 for account-mode/login. This confirms those targeted route outcomes only; it is not production acceptance.
- At frozen code checkpoint `06b80bad0035cc0716b08592b2c6ac241a48632a`, gates are verified: full Ruff green; strict mypy over 961 source files green; 548 adversarial tests; 61 focused memory tests; 54 frontend tests; frontend lint and build green. Independently, reviewer approval of the known memory fixes and 16 focused memory tests passed. These code gates do not close the capability acceptance gaps below or establish deployment/production acceptance.
- The source checkpoint and tree identity are verified, while movement to main and branch/worktree cleanup remain pending root action. Final main status must not be inferred here.

## Remaining capability work

- **Account authority and enrollment:** `arctrust` has signed enrollment grant primitives and Vault anchor/transit adapters, but the production account-authority factory, trusted enrollment/configuration, lease and strict-audit composition are not turnkey. Until configured, expose a typed unavailable result at public seams that define one; do not silently use memory authority. Production host identity, signing authority, anchor and audit custody still need real composition and verification.
- **Hosted grant, setup, and release:** Arc Cloud recovery is on its separate `main` at `23e7fbc`, including recovery `1ef0a6c`; the cloud docs handoff is `05ee143`, and original snapshot `e4529a3` remains reachable in its history. Source recovery is present, while real grant, customer setup, and release acceptance remain open. Keep readiness distinct from setup and do not report launch availability until those steps are verified.
- **Queue metadata and controls:** ArcLLM has queue admission/control types and a journal, but durable metadata listing is not yet bounded and tenant-scoped. `QueueJournal._list_jobs` in `packages/arcllm/src/arcllm/queue_journal.py` reads all rows before tenant filtering and page slicing. Complete an indexed, bounded, tenant-authorized design that does not reveal foreign-tenant counts or cursors. Durable accepted-run intent, request/result ownership, and crash-safe replay acceptance remain separate open work. Until the listing and authority path are ready, controls should return their typed unavailable result rather than imply complete or correctly bounded visibility.
- **Skill revision and reports:** The skill handoff describes signed anchored bundle activation and isolated report preview, but production anchor/signer factories, authenticated startup composition, and the deployment-scoped report read policy remain acceptance gates. Preserve unavailable behavior where those authorities are absent.
- **Memory automatic promotion:** The current code raises `MemoryPromotionUnavailableError` while verified score grant and trusted source/fleet attachment are unavailable. The nightly consolidation enabled-promotion path raises the same typed unavailable result; the default disabled baseline remains. This is an unavailable behavior under test, not completed automatic promotion. Acceptance still requires trusted score/source composition, a stable source-revocation fence, and real fleet attach/detach verification.
- **Shared-knowledge entity promotion:** Canonical cross-agent or multi-contributor entity promotion raises `SharedKnowledgeUnavailableError` pending a signed contributor and provenance design. Ordinary signed-document promotion remains available. SPEC-083 is incomplete even as its branch source is consolidated; do not treat shared-knowledge entity promotion as shipped.
- **Approval release and reconciliation:** Current source implements signed, pinned operator decisions, excludes private bodies from listing rows, validates the current export/digest, uses compare-and-set claims, and avoids automatic replay after an uncertain outcome. This implementation is under final review. An operator reconciliation UI for `releasing` and `outcome_unknown` claims remains open.
- **Skill-improvement event bridge:** The run bridge maps `tool.end` to `agent:post_tool`, while the skill hook requires a compatible success payload and an explicit error mapping. Do not claim outcome capture or promotion until the event bridge and route-to-runtime path are verified.

## Resume and acceptance

Continue all remaining work against main after root records the final integration checkpoint. Preserve the package seams and existing typed unavailable behavior; do not add a memory fallback for missing authoritative storage. Close the account/enrollment, hosted grant/setup/release, bounded tenant-scoped durable queue metadata, trusted memory score/source and fleet attachment/revocation fence, approval-claim reconciliation UI, and skill-improvement event bridge gaps with focused evidence. Keep deployment status separate: the known DGX evidence is for `fda6991f`, and later main changes require their own deployment and sync/load evidence.

Root will append the exact merged hash, final gates, and removed-branch/worktree results to this handoff once verified. Do not infer any of them before that update.

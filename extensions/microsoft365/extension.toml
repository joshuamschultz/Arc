# Microsoft 365 (Outlook mail, calendar, OneDrive) over MCP.
#
# MCP is the right transport here and not a fallback. There is no acceptable
# Microsoft 365 CLI, and the Graph surface is 324 endpoints wide — which is
# exactly why `allow` below is short and explicit. An omitted allowlist is the
# same unbounded grant as "*" and is refused above personal tier.

[extension]
name = "microsoft365"
display_name = "Microsoft 365"
version = "1.0.0"
description = "Read and send Outlook mail, manage the calendar, and browse OneDrive files."
attachment = "mcp"
tier_floor = "personal"

# The published npm tarball, digest computed over the artifact itself. Keyed
# `any` because an npm tarball is the same bytes everywhere, and carrying no
# `member` because npm installs it rather than it being an archive holding one
# executable — so `arc connector host-setup` says plainly that it cannot place
# this on PATH and hands over the steps below.
[artifact]
package = "@softeria/ms-365-mcp-server"
version = "0.136.0"

[artifact.platforms.any]
url = "https://registry.npmjs.org/@softeria/ms-365-mcp-server/-/ms-365-mcp-server-0.136.0.tgz"
sha256 = "bf953dae54cef1c2ea616b964f5b72beb8910f22b6e84c60cd94c14b6935b232"

[[host_requires]]
name = "node"
minimum_version = "20.0.0"
instruction = "Install Node.js 20 or newer on this host; the MCP server is a Node program."

# No `token_command`: `--login` runs the Entra ID device-code flow, which prints
# a code and waits for a person to enter it in a browser. There is no token to
# pipe in, so the command is shown and never attempted.
#
# `--verify-login` is the sign-in check. It needs a `verify_pattern` because it
# exits 0 either way: it prints the JSON its own testLogin() returned, which is
# {"success":false,...} when there is no usable token.
[[host_requires]]
name = "ms-365-mcp-server"
minimum_version = "0.136.0"
authorize_command = "ms-365-mcp-server --login"
verify_command = "ms-365-mcp-server --verify-login"
verify_pattern = '"success"\s*:\s*true'
instruction = """
Install the server on this host, verify it, then authorise it:

  1. npm pack @softeria/ms-365-mcp-server@0.136.0
  2. echo 'bf953dae54cef1c2ea616b964f5b72beb8910f22b6e84c60cd94c14b6935b232  softeria-ms-365-mcp-server-0.136.0.tgz' | sha256sum -c -
  3. npm install -g ./softeria-ms-365-mcp-server-0.136.0.tgz
  4. Register an Entra ID application. You do not need to export anything: paste
     its client ID, tenant ID and client secret into the connector panel and Arc
     puts them in this server's environment for you.
  5. ms-365-mcp-server --login    # only if you are signing in AT this host

Install it, do not `npx` it. `npx -y <pkg>` resolves and executes whatever the
registry serves at the moment of launch, which makes the digest in [artifact]
describe bytes that are not the bytes that ran.
"""

# These three were declared and delivered nowhere: the server reads them from its
# own environment and nothing put them there. [secrets.placement] is that missing
# half. The variable names are this server's, taken from its README, and they live
# here so that a rename upstream is a change to this file and to no other.
#
[[secrets]]
name = "client_id"
prompt = "Application (client) ID of your app registration. In the Azure portal open Microsoft Entra ID, then App registrations, then your app; it is the 'Application (client) ID' on the Overview page. Looks like 11111111-2222-3333-4444-555555555555."
sensitive = false

[secrets.placement]
variable = "MS365_MCP_CLIENT_ID"

[[secrets]]
name = "tenant_id"
prompt = "Directory (tenant) ID, on the same Overview page as the client ID. If this app should work for any organisation, type: common"
sensitive = false

[secrets.placement]
variable = "MS365_MCP_TENANT_ID"

[[secrets]]
name = "client_secret"
prompt = "Client secret for that app registration. In the Azure portal open your app, then 'Certificates & secrets', click 'New client secret', give it any description, and copy the VALUE column (not Secret ID) immediately — Azure never shows it again."
format = "api_token"

[secrets.placement]
variable = "MS365_MCP_CLIENT_SECRET"

[tools]
# Nine names out of 324. Every one of them is a name this manifest chose; a verb
# the server starts serving that is not on this list can never register.
allow = [
  "list-mail-messages",
  "get-mail-message",
  "list-mail-folders",
  "send-mail",
  "list-calendar-events",
  "get-calendar-view",
  "create-calendar-event",
  "list-folder-files",
  "search-onedrive-files",
]

[[tools.declared]]
name = "list-mail-messages"
description = "List Outlook messages in the signed-in mailbox."
classification = "read_only"

[[tools.declared]]
name = "get-mail-message"
description = "Read one Outlook message, including its body."
classification = "read_only"

[[tools.declared]]
name = "list-mail-folders"
description = "List the mail folders in the mailbox."
classification = "read_only"

# The one sending verb in the set. Tagged so the egress gate (D-580) sees it,
# which also means this bundle is refused at federal tier at install.
[[tools.declared]]
name = "send-mail"
description = "Send an Outlook email to the named recipients."
classification = "state_modifying"
capability_tags = ["network_egress"]

[[tools.declared]]
name = "list-calendar-events"
description = "List calendar events."
classification = "read_only"

[[tools.declared]]
name = "get-calendar-view"
description = "Read the calendar over an explicit time window, recurrences expanded."
classification = "read_only"

# Creating an event mails an invitation to every attendee the model names, so it
# is an egress verb even though it reads like a write.
[[tools.declared]]
name = "create-calendar-event"
description = "Create a calendar event. Attendees are invited by email."
classification = "state_modifying"
capability_tags = ["network_egress"]

[[tools.declared]]
name = "list-folder-files"
description = "List files in a OneDrive folder."
classification = "read_only"

[[tools.declared]]
name = "search-onedrive-files"
description = "Search OneDrive files by name and content."
classification = "read_only"

[approval]
default = "outbound"

# `--read-only` and `--enabled-tools` are the
# server's own controls, applied as defence in depth behind the `allow` list
# above: the manifest bounds what may register, and these bound what the server
# is willing to serve in the first place.
[config.mcp]
transport = "stdio"
argv = [
  "ms-365-mcp-server",
  "--enabled-tools",
  "^(list-mail-messages|get-mail-message|list-mail-folders|send-mail|list-calendar-events|get-calendar-view|create-calendar-event|list-folder-files|search-onedrive-files)$",
  "--allowed-scopes",
  "User.Read Mail.Read Mail.Send Calendars.ReadWrite Files.Read",
]
client_name = "arc"

[config.mcp.resilience]
timeout_seconds = 60.0
max_attempts = 3

[config.mcp.tools.list-mail-messages]
classification = "read_only"

[config.mcp.tools.get-mail-message]
classification = "read_only"

[config.mcp.tools.list-mail-folders]
classification = "read_only"

[config.mcp.tools.send-mail]
classification = "state_modifying"
capability_tags = ["network_egress"]

[config.mcp.tools.list-calendar-events]
classification = "read_only"

[config.mcp.tools.get-calendar-view]
classification = "read_only"

[config.mcp.tools.create-calendar-event]
classification = "state_modifying"
capability_tags = ["network_egress"]

[config.mcp.tools.list-folder-files]
classification = "read_only"

[config.mcp.tools.search-onedrive-files]
classification = "read_only"

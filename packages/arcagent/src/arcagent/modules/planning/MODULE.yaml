name: planning
version: 0.1.0
description: Personal task notebook — track multi-step work across turns
author: ArcAgent
entry_point: arcagent.modules.planning:PlanningModule
dependencies:
  - arcagent.core.module_bus
events:
  subscribes:
    - agent:assemble_prompt
    - agent:shutdown

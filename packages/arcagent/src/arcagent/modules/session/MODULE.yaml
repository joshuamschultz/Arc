name: session
version: 0.1.0
description: JSONL session store wrapper + SQLite FTS5 derived index for full-text search across past sessions
author: ArcAgent
entry_point: arcagent.modules.session:SessionModule
dependencies:
  - arcagent.core.session_manager
  - arcagent.core.tool_registry
events:
  subscribes: []
  emits:
    - session.search.queried
tools:
  - session_search

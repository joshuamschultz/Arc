"""Telegram messaging module — bidirectional human-agent interaction via Telegram Bot API.

Exposed to the agent through the decorator-form capabilities in
:mod:`arcagent.modules.telegram.capabilities`; per-agent runtime state
(the :class:`~arcagent.modules.telegram.bot.TelegramBot`, config, telemetry)
lives in :mod:`arcagent.modules.telegram._runtime`.
"""

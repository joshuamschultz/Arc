[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "arccmd"
version = "0.9.0"
description = "Unified CLI for Arc products (arcllm, arcrun, arc-agent)"
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]
dependencies = [
    "arcokf>=0.2",
    "click>=8.0",
    "arcllm",
    "arcrun",
    "arc-agent",
    "arcbundle",
    "arcteam",
    "python-dotenv>=1.2.2",
    "prompt_toolkit>=3.0",
    "tomlkit>=0.13,<1",
]

[project.scripts]
arc = "arccli.main:main"
arc-agent-worker = "arccli.agent_worker:main"

[project.urls]
Homepage = "https://github.com/joshuamschultz/Arc"
Repository = "https://github.com/joshuamschultz/Arc"
Issues = "https://github.com/joshuamschultz/Arc/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/arccli"]

[tool.ruff]
target-version = "py311"
line-length = 99

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "UP", "B", "A", "S", "T20", "RUF"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101", "S105", "S106", "S108", "S603", "E402", "E501", "N806", "E741", "B007"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--import-mode=importlib"
consider_namespace_packages = true
# The workspace root, so a test importing a sibling helper by its full path
# (`packages.arccli.tests.conftest`) resolves the same whether pytest was
# pointed at this package or at the whole repo. Uniform across every package:
# the moment one keeps prepend mode, a bare `tests` resolves again from its own
# rootdir and the whole-repo run dies in collection.
pythonpath = ["../.."]
asyncio_mode = "auto"
